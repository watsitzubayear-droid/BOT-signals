# =============================================================================
# QOTEX PRO SIGNAL GENERATOR v3.1 - REQUIREMENTS.TXT
# Minimum Python Version: 3.9+
# =============================================================================

# --- CORE WEB FRAMEWORK ---
streamlit>=1.30.0          # Advanced web UI framework for real-time dashboards

# --- DATA SCIENCE & ANALYSIS ---
pandas>=1.5.0              # High-performance data manipulation and analysis
numpy>=1.24.0              # Numerical computing and random number generation

# --- ADVANCED VISUALIZATION ---
plotly>=5.15.0             # Interactive charts (heatmaps, scatter plots, pie charts)
plotly-express>=0.4.0      # Simplified Plotly syntax for quick chart generation

# --- SYSTEM & UTILITIES ---
python-dateutil>=2.8.2     # Date/time parsing and timezone handling
pytz>=2023.3               # World timezone definitions

# --- INSTALLATION COMMANDS ---
# Install all dependencies: pip install -r requirements.txt
# Upgrade all packages: pip install --upgrade -r requirements.txt
# Install without cache: pip install --no-cache-dir -r requirements.txt

# --- OPTIONAL PERFORMANCE BOOSTERS ---
# numba>=0.58.0              # Just-in-time compilation for faster calculations (optional)
# bottleneck>=1.3.7          # Fast NumPy array functions (optional)

# --- SECURITY NOTE ---
# All packages are verified and compatible with Streamlit Cloud deployment
